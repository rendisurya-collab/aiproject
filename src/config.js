// API URL: in production both frontend and backend are served from same origin
// In development, Vite proxy handles /api and /uploads forwarding to localhost:5000
export const API_URL = '';
export const UPLOADS_URL = '';
